#include <bits/stdc++.h>

using namespace std;

int main()
{
    ​ios::sync_with_stdio(0); cin.tie(0);
    deque < int> A;
    vector < pair<int , int>> parejas;
    long long n,a,m,querie,B,C;
    cin>>n>>m;
    for(int i= 0 ;i < n; i++)
    {   cin >> a;
        A.push_back(a);
    }
    parejas.push_back(make_pair(A[0],A[1]));
    for(int i = 0;i < m;i++)
    {
       
    }
    return 0;
}